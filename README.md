# QA Test

Имитация тестирования API авторизации.

---

## ⚙️ Установка
```bash
git clone
cd test
pip install -r requirements.txt
Запуск тестов

-pytest

или с HTML-отчётом:

-pytest --html=report.html